package net.pickapack.spider.noJs.crawler.xml;

public class NodeRule extends Rule {
    public NodeRule(String id, String path, String applyUrlPatternIds, boolean follow, boolean download, boolean print) {
        super(id, path, applyUrlPatternIds, follow, download, print);
    }
}
